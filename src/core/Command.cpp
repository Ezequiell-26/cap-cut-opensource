#include "Command.hpp"

namespace ccos::core {

// Command está implementado completamente en el header como una clase base abstracta
// con métodos virtuales puros y algunos métodos inline. Las clases derivadas
// (como EditCommands) deben implementar los métodos puros execute(), undo(), name().

} // namespace ccos::core
