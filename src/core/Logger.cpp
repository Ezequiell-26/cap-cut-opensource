#include "Logger.hpp"

namespace ccos::core {

// Logger está implementado completamente en el header como un singleton
// con métodos inline. Este archivo existe para consistencia con la estructura
// del proyecto y para posibles extensiones futuras.

// Todas las funcionalidades de logging (ConsoleLogSink, FileLogSink, Logger)
// están implementadas como inline en el header para eficiencia.

} // namespace ccos::core
